{
  "name": "wtp-firestore-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-wtp-survey --config ../../firebase.json \"node --test firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.4.0",
    "firebase-tools": "15.28.2"
  }
}
